# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=permission_system

# Redis 配置
REDIS_URL=redis://localhost:6379

# JWT 配置
JWT_SECRET=permission-system-secret-key

# 运行环境
NODE_ENV=development
