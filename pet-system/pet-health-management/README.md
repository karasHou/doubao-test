# 宠物健康管理工具

一个基于 React + Node.js + PostgreSQL 的全栈宠物健康管理应用，支持宠物信息管理、疫苗/体检记录、定时提醒等功能。

## 功能特性

- 🐶 🐱 支持猫和狗两种宠物类型
- 📝 记录疫苗接种和体检历史
- ⏰ 定时提醒功能（疫苗接种、体检等）
- 📊 健康数据统计分析
- 🐾 多宠物管理支持
- 🎨 根据宠物类型生成SVG形象

## 技术栈

- **前端**: React 18 + Vite + Tailwind CSS + Axios
- **后端**: Node.js + Express + PostgreSQL
- **定时任务**: node-cron
- **部署**: Docker + Docker Compose

## 快速开始

### 环境要求

- Docker
- Docker Compose

### 一键部署

```bash
cd pet-health-management
docker-compose up -d
```

应用启动后访问：http://localhost

### 手动启动

1. **启动后端**
```bash
cd backend
npm install
npm run dev
```

2. **启动前端**
```bash
cd frontend
npm install
npm run dev
```

## 项目结构

```
pet-health-management/
├── backend/                # Node.js 后端服务
│   ├── src/
│   │   ├── models/         # 数据库模型和连接
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 业务逻辑服务
│   │   └── app.js          # 应用主文件
│   └── package.json
├── frontend/               # React 前端应用
│   ├── src/
│   │   ├── components/     # React 组件
│   │   ├── services/       # API 服务
│   │   └── App.jsx         # 主应用组件
│   └── package.json
├── docker-compose.yml      # Docker Compose 配置
└── README.md
```

## API 接口

- `GET /api/pets` - 获取所有宠物
- `POST /api/pets` - 添加新宠物
- `GET /api/vaccinations/pet/:petId` - 获取宠物疫苗记录
- `POST /api/vaccinations` - 添加疫苗记录
- `GET /api/checkups/pet/:petId` - 获取宠物体检记录
- `POST /api/checkups` - 添加体检记录
- `GET /api/reminders` - 获取所有提醒
- `POST /api/reminders` - 创建新提醒

## 数据结构

### Pets 表
存储宠物基本信息，包括姓名、类型、品种、生日、体重等。

### Vaccinations 表
记录宠物疫苗接种历史，包括疫苗名称、接种日期、下次接种日期等。

### Checkups 表
存储体检记录，包括体检日期、诊断结果、体重等。

### Reminders 表
管理各种提醒任务，包括疫苗接种提醒、体检提醒等。

## 定时任务

使用 `node-cron` 库实现定时任务，每分钟检查一次需要发送的提醒。

## License
MIT